import { createTheme } from "@mui/material/styles";

// Create a theme instance.
export const theme = createTheme({
	palette: {
		primary: {
			main: "#0F0F0F",
		},
		secondary: {
			main: "#F0572F",
		},
		background: {
			default: "#F3F3F3",
		},
	},
});
